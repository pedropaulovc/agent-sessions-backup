node_modules/
.wrangler/
dist/
*.log
.dev.vars
.env
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
collector/.coverage
infra/out/
.DS_Store
.claude/worktrees/

# local secrets — never commit
.env.local
*.pfx
*.cer
*.client.key

# Playwright CLI page snapshots: these capture whatever was on screen, which for this
# project is real session data (prompts, machine names, local paths, session UUIDs) served
# from the authenticated viewer. Never commit them.
.playwright-cli/
